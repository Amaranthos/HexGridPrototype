using UnityEngine;
using System.Collections;

public class FPS : MonoBehaviour {

	void OnGUI() {
		// float dT = Mathf.Round(1/Time.deltaTime);
		// GUI.Label(new UnityEngine.Rect(5,5,200,100), dT.ToString() + " fps " + (Time.deltaTime * 1000).ToString() + " ms");
	}
}
